print('Пирамидка.')
N = int(input('Введите высоту пирамидки: '))
col = 2 * N - 1
for row in range (1, N+1):
    blocks = (2 * row - 1)
    print(' '* ((col - blocks) // 2)  + '#' * blocks  + ' '* ((col - blocks) // 2)) 
